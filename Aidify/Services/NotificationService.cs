namespace Aidify
{
    // STUB — full implementation is Manoj's task in Phase B2.
    // Other developers can call this now; it is a safe no-op until implemented.
    public static class NotificationService
    {
        public static void Push(int userId, string title, string body, string url = null)
        {
            // TODO (Manoj): replace this stub with INSERT into Notifications
        }
    }
}
